local TD = TD.td1madao["ydj_dst_td1madao"] TD.createFn("fori", function(arr, fn)     if TD.isTab(arr) and TD.isFn(fn) then         for k, v in pairs(arr or {}) do             local canBreak =fn(k, v)             if canBreak then                 break             end         end     elseif TD.isFn(fn) then         if not TD.isPos(arr) then             arr = 65535         end         for i = 1, arr do             local canBreak = fn(i)             if canBreak then                 break             end         end     end end) 